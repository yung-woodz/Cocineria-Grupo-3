/*
import TableOrder from '@hooks/table/TableOrder.jsx';

export default function Allorders() {
    const {TableOrder} = useTable({ data, columns, initialSortName, onSelectionChange });

    return (
        <div className='table-container'>
        <div ref={tableOrder}></div>
        </div>
    );
    }
*/